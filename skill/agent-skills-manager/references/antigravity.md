# Antigravity

Load when the request names Antigravity, its agent profile, rules, Skills, or MCP configuration.

Determine the intended project or user scope before locating files. Use `agent-skills-manager` exclusively for Skills synchronization. Handle MCP servers and all other configuration with a prompt-guided edit. Do not assume Antigravity paths, schema, or precedence; inspect the installed product and consult its official documentation when uncertain.

Read the active configuration, confirm scope and the exact target block, display a minimal diff, create a timestamped sibling backup, edit only that block, validate with the product's supported mechanism, and re-read it. Do not disturb unrelated configuration or secrets.
